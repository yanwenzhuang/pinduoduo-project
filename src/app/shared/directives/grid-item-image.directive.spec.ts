import { GridItemImageDirective } from './grid-item-image.directive';

describe('GridItemImageDirective', () => {
  it('should create an instance', () => {
    const directive = new GridItemImageDirective();
    expect(directive).toBeTruthy();
  });
});
