import { GridItemTitleDirective } from './grid-item-title.directive';

describe('GridItemTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new GridItemTitleDirective();
    expect(directive).toBeTruthy();
  });
});
